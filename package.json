{
  "name": "web-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "mocha": "^9.0.1",
    "typescript": "^4.3.4"
  },
  "scripts": {
      "prebuild": "rm -rf dist",
      "build": "yarn test && tsc",
    "test": "mocha -r ts-node/register 'tests/**/*.spec.ts'"
  },
  "devDependencies": {
    "@types/chai": "^4.2.18",
    "@types/mocha": "^8.2.2",
    "chai": "^4.3.4",
    "ts-node": "^10.0.0"
  }
}
